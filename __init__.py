"""mRNA-EditFlow (MEF): edit-native, variable-length mRNA generation & design.

A frozen mRNA-native foundation-model encoder + a lightweight edit-flow
generation head, trained under a region-conditioned edit grammar
(CDS = frame-locked codon lattice; UTR = free-length regulatory canvas).
"""
__version__ = "0.1.0"
