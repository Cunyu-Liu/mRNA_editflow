# Route2 SetFlow 组合实验（V6.5 × β=0.25 机制验证）预注册 v1

> **状态：PREREGISTRATION（2026-09-11，用户拍板：「执行组合实验验证机制」+ 论文正结果主线）**。触发 = 口径镜像分歧现象（V6.5：recovery PASS + Optimus FAIL；β=0.25：recovery FAIL + Optimus PASS）——两个单口径正信号的机制叠加性未测。

## 1. 科学问题（预冻结）

**Q-COMB**：探索宽度（coverage-排序联合增益，V6.5 机制）与弱势能引导（Optimus 功能增益，β=0.25 机制）是否**可叠加**？

- 假设 H-add：组合 = 纯探索 + 纯引导 的增益之和（两机制独立作用于不同维度：池构成 vs 转移偏置）
- 假设 H-int：干扰——引导改变采样分布使 genscore 选回偏向引导方向（探索增益被吞噬）或反之
- 假设 H-ind：独立（组合臂在两口径上分别复制各自单臂的增益，无交互项）

## 2. 2×2 机制矩阵（全部 891 全量、预注册）

| 臂 | 探索（B） | 引导（β） | 提交 | 数据状态 |
|---|---|---|---|---|
| A 基线 | 32 | 0 | 全 32 | **已有**（guided_b2_20260903 unguided） |
| B 纯引导 | 32 | 0.25 | 全 32 | **已有**（beta_full891_20260909/beta_0.25_full） |
| C 纯探索 | 256 | 0 | genscore top-32 | **已有**（pool256 + V6.5 收割） |
| **D 组合** | **256** | **0.25** | **genscore top-32** | **待发射**（唯一新数据） |

注意：B 臂的 Optimus 数字已有（β=0.25 guided = +0.036 排零正，批 62 双口径产物）；C 臂三口径数字已有（V6.5 收割）。**D 臂是唯一需要新采样的格子**。

## 3. 判定门（amendment v2 口径，预注册）

**机制判定规则（发射前冻结）**：
- **叠加成立（H-add）**：D 臂 Δsc-hit@1 ≥ +0.03 CI 排零（复制 C 的 B2 门）**且** D 臂 ΔOptimus ≥ +0.02 CI 排零（复制 B 的 B2-I 门）——两口径同时过 = 机制正交叠加
- **干扰（H-int）**：D 在任一口径显著低于对应单臂（配对 ΔΔ CI 排零负）
- **独立（H-ind）**：D 复制两单臂增益但无显著超加
- 两级设计：calib100 先行（D 臂一臂，~13min）→ 任一口径方向阳性（点估计为正）→ 891 全量 + 3-seed；双负 → 收口报告（机制否证也是论文素材——干扰结构刻画）

**第三口径**：sc-hit@1、recovery、support（recovery 家族）+ Optimus top-1 delta（独立口径）+ polyA APARENT（n=20 仅报告）。

## 4. 执行计划

1. D 臂 calib100（`--trajectory-count 256 --beta 0.25 --critic-kind v5 --arms guided`）——低峰 watcher
2. calib100 三口径快算（含 Optimus）→ 阳性 → 891 全量 D 臂 + seed 副本 ×2
3. 终判：2×2 矩阵全表（四臂 × 三口径）+ 机制假设判定 + journal 终批
4. 论文素材：正结果主线下的机制章节（两正信号叠加性 + 口径分歧现象学解释）
