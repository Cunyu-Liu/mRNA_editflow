# XEdit V3 interim Development evidence

Status date: 2026-08-24. This note is an observed-results companion to the prospectively frozen protocol. It does not amend the architecture, seeds, thresholds, task set, baselines, selection order, or authorization boundary in the protocol. Development TEST and new final Evaluation outcomes remain unread.

## Interpretation boundary

The Critic screen is still incomplete because the C3 full arm and its four preregistered controls are running; no C3 performance metric has been read. C0 and C1 are not selectable Critic arms; the C2 full arm and all four controls are terminal. C2 is ineligible on both its primary criteria and the edit-metadata control. For SetFlow, F0 is a read-only historical reference and F1 is an objective diagnostic that cannot be selected. Both selectable arms have completed: F2 failed its frozen arm gate on unique-candidate rate, and F3 failed recovery, top-k recovery, and unique-candidate rate. Neither arm can enter confirmation.

## Critic diagnostic evidence

| Arm | Status | Task-macro Spearman | Margin over C0 | Standardized MAE | Positive tasks | Frozen interpretation |
|---|---|---:|---:|---:|---:|---|
| C0 endpoint-aware raw CNN | Terminal | 0.1108180590 | 0 | 1.9924297611 | 8/9 | Matched same-information baseline only. Ranking is weak and MAE exceeds 1.70. |
| C1 C0 + global mRNABERT mean residual | Terminal | 0.1386460633 | +0.0278280043 | 1.9004665151 | 8/9 | Global residual gives a small diagnostic improvement, but C1 is nonselectable and misses the 0.25 Spearman and 1.70 MAE thresholds. No retraining is authorized. |
| C2 frozen edit-site token critic | Full arm and 4/4 controls terminal | 0.1042656112 | -0.0065524478 | 1.9705208102 | 7/9 | The full arm fails Spearman, margin, MAE and positive-task thresholds and cannot become eligible. Its completed controls do not rescue eligibility. |
| C2 source-only control | Terminal control | -0.0013102930 | not applicable | 1.9220229682 | 3/9 | The full model beats source-only, so candidate-side information is used, but this does not rescue the failed primary gate. |
| C2 edit-metadata-only control | Terminal control | 0.1078162132 | -0.0030018458 versus C0 | 1.9265768541 | 7/9 | The full model is 0.0035506020 lower in Spearman and 0.0439439561 worse in standardized MAE; therefore it fails the frozen requirement to beat this control. |
| C2 no-candidate-sequence control | Terminal control | 0.0384855077 | -0.0723325513 versus C0 | 1.8590261707 | 4/9 | The full model is 0.0657801035 higher in Spearman but 0.1114946395 worse in standardized MAE. Candidate sequence adds aggregate ranking information, not a qualifying primary result. |
| C2 candidate-bundle permutation control | Terminal control | 0.0592276162 | -0.0515904428 versus C0 | 1.9698397875 | 7/9 | The full model is 0.0450379950 higher in aggregate Spearman. The frozen exact applicable-task win count remains delegated to the current-HEAD formal gate and is not inferred from aggregate metrics. |

C1 therefore supports only the narrow observation that the legacy global pretrained residual contains some Development signal. C2 underperforms C0 by 0.0066 Spearman, exceeds the 1.70 MAE ceiling, and has only seven positive tasks. Its full model beats source-only and no-candidate-sequence controls in aggregate Spearman, and exceeds the complete candidate-bundle permutation control by 0.0450 aggregate Spearman, so candidate-side information is used. However, the edit-metadata-only control reaches 0.1078 Spearman and 1.9266 standardized MAE, outperforming the full model on both measures; the full model also has worse MAE than the no-candidate-sequence control and is essentially tied with the permutation control on MAE. The evidence therefore does not establish incremental ranking value from the candidate-sequence/edit-site branches beyond edit metadata. C2 fails both its primary criteria and a required control comparison. C3 is the only remaining architecture that could pass the Critic screen; its full arm and four controls are now executing on the synchronized launch head, but no active result is available for interpretation. The exact C2 permutation comparison across the six applicable tasks is reserved for the formal gate and is not replaced by this aggregate diagnostic.

Primary repository evidence: `audits/route_a_v3_route2_xeditcritic_v3_c1_terminal_c2_launch_v1.json`, `audits/route_a_v3_route2_xeditcritic_v3_c2_full_source_terminal_controls_running_v1.json`, `audits/route_a_v3_route2_xeditcritic_v3_c2_edit_metadata_terminal_v1.json`, and `audits/route_a_v3_route2_xeditcritic_v3_c2_control_package_terminal_v1.json`. The authoritative full artifacts remain under the Route 2 `/mnt` experiment root and the central attempt ledger.

## SetFlow diagnostic evidence

| Arm | Status | Common set-NLL | Relative NLL improvement over F0 | Recovery | Top-k recovery | Unique rate | Correctness | Frozen interpretation |
|---|---|---:|---:|---:|---:|---:|---|---|
| F0 terminal Base Flow V2 replay | Frozen reference | 5.3979076352 | 0 | 0.203 | 0.098 | 0.883 | Historical G0 ready | Read-only comparator; not retrained. |
| F1 small trunk + set-marginal objective | Terminal diagnostic | 5.4724267445 | -0.0138051842 | 0.2691732136 | 0.2131454877 | 0.4192269921 | Legality 1.0; budget, replay and numerical failures 0 | Recovery and top-k improve, but NLL worsens and diversity collapses. F1 is nonselectable and terminal. |
| F2 eight-block hybrid, width 384 | Terminal; arm gate failed | 2.0680908164 | +0.6168717666 | 0.2924616536 | 0.1682782203 | 0.6793630752 | Legality 1.0; budget, replay and numerical failures 0 | NLL, recovery, top-k and G0 checks pass, but unique rate misses 0.90. F2 is terminal and cannot enter confirmation. |
| F3 twelve-block hybrid, width 512 | Terminal; arm gate failed | 2.0504294127 | +0.6201436647 | 0.1939768051 | 0.1048712807 | 0.6374508979 | Legality 1.0; budget, replay and numerical failures 0 | NLL and G0 checks pass, but recovery, top-k and unique rate all miss their frozen thresholds. F3 cannot enter confirmation. |

F1 supplies useful mechanism evidence: an order-invariant set target can improve measured-candidate recovery even when the small legacy trunk remains severely mode-concentrated. F2 shows that the larger hybrid trunk can strongly improve common set-NLL and clear the recovery/top-k thresholds, but its source-macro unique rate remains only 0.6794. F3 training selected pass 3 after five completed passes and reached a common set-NLL of 2.0504, a 62.01% improvement over F0, but its generation recovery fell to 0.1940 and its unique rate to 0.6375. Thus increased capacity improved likelihood without producing the frozen recovery/diversity profile. Because neither selectable arm passed, the SetFlow V3 screen is terminal `XEDITSETFLOW_V3_SCREEN_NO_GO`; the current-HEAD formal gate artifact has now been materialized with no selected arm and `NO_SELECTABLE_ARM_PASSED`. No confirmation seed, retraining, threshold reduction, or architecture substitution is authorized under the frozen protocol.

The budget-stratified terminal diagnostic makes this failure sharper. F3 candidate recovery for B1/B3/B5 is 0.4465/0.1689/0.0126, compared with F2's 0.4857/0.3783/0.0550; F3 top-k recovery is 0.2356/0.0969/0.0063, compared with 0.2600/0.2367/0.0296; and F3 unique rate is 0.4588/0.6560/0.7650, compared with 0.4832/0.7134/0.8066. F3 is therefore worse than F2 on all three generation measures within every edit budget despite its slightly better common set-NLL. In the domain view, F3 recovery is 0 for GSE269595, 0.0370 for ENCSR854RUF, 0.0405 for GSE217518, and 0.2520 for GSE114002; every domain's mean unique rate remains below 0.90. Because GSE114002 contributes 652 of 891 sources, the aggregate recovery is composition-sensitive. These patterns are consistent with likelihood-to-generation misalignment and candidate concentration, rather than a problem that parameter-count growth alone repaired; they do not authorize a post hoc replacement model.

An outcome-free post-terminal decomposition shows that F2's diversity failure is not confined to the one-edit budget. Mean source-level unique rates for B1, B3, and B5 are 0.4832, 0.7134, and 0.8066, respectively; the fractions of sources reaching 0.90 are 0%, 22.0%, and 35.8%. B1 is the most concentrated regime, but every edit budget remains below the frozen diversity target. The appropriate interpretation is therefore that F2 improves distributional fit and measured recovery without resolving candidate concentration across the full budget range.

The same outcome-free diagnostic by evaluable study/endpoint domain gives mean unique rates of 0.4359 for GSE269595/poly(A)-usage, 0.6581 for GSE114002/mean-ribosome-load, 0.7063 for ENCSR854RUF/allelic-skew, and 0.8221 for GSE217518/RNA-half-life. Every domain remains below 0.90, so the failure is not attributable to a single study. Study and endpoint are one-to-one confounded in this validation cohort and must not be interpreted as independently estimated endpoint-semantic effects. GSE114002 contributes 652 of 891 sources, so the overall source-macro value is also composition-sensitive; domain-resolved values should accompany the aggregate benchmark result.

Primary repository evidence: `audits/route_a_v3_route2_xeditsetflow_v3_f1_terminal_diagnostic_v1.json`, `audits/route_a_v3_route2_xeditsetflow_v3_f1_training_terminal_20260823_075027.json`, `audits/route_a_v3_route2_xeditsetflow_v3_f2_terminal_f3_health_v1.json`, `audits/route_a_v3_route2_xeditsetflow_v3_f2_diversity_diagnostic_v1.json`, `audits/route_a_v3_route2_xeditsetflow_v3_f2_diversity_domain_diagnostic_v1.json`, `audits/route_a_v3_route2_xeditsetflow_v3_f2_f3_budget_domain_recovery_diagnostic_v1.json`, `audits/route_a_v3_route2_xeditsetflow_v3_f3_training_terminal_validation_launch_v1.json`, `audits/route_a_v3_route2_xeditsetflow_v3_f3_terminal_screen_no_go_v1.json`, `audits/route_a_v3_route2_a100_current_head_sync_tests_setflow_gate_v1.json`, and `docs/execution/route_a_v3_route2_training_attempt_table_20260817.md`.

## Current claim state

No V3 model-performance claim is established. The project remains not ready for submission, critic guidance remains unauthorized, and the external Evaluation remains locked. A strong critic claim still requires the remaining selectable C3 arm to pass the screen, followed by exact three-seed confirmation, atomic Development TEST, all-Development refit, and LOSO readiness. The terminal C2 result cannot regain eligibility. The frozen SetFlow method-repair round cannot support the requested generator claim because no selectable base-flow arm passed screen; consequently three-seed Flow confirmation and soft-value SMC are not authorized in this round.

This interim evidence should be replaced by a terminal screen table only after every preregistered screen artifact required by its gate is present. Failed results remain terminal evidence; thresholds, tasks, seeds, and baselines are not changed in response to these observations.

## Prospective V4 implementation status

The V4 SetFlow confirmation execution path is implemented but has produced no model-performance result. It accepts only the preregistered full model and seeds 20260912, 20260913, and 20260914 after a complete SetFlow V4 screen PASS. Each seed retains the frozen four-checkpoint outcome-free Validation selection rule and must independently clear the absolute generation gates, the frozen margins over terminal F2, and a 10,000-replicate paired bootstrap recovery-improvement interval whose lower 95% bound is strictly positive on the identical 891-source cohort. A technical failure or failure of any seed is terminal; no additional seed is authorized.

This implementation does not alter the current claim state. SetFlow V4 training and Validation generation have not started, no V4 checkpoint has been selected, and no generator advantage has been observed. `XEDITSETFLOW_V4_G0_READY`, if eventually reached, is necessary but not sufficient for guidance: formal guidance remains locked unless Critic V4 independently reaches `CRITIC_V4_READY_FOR_GUIDANCE`. Development TEST and new final Evaluation outcomes remain unread.

Critic V4 three-seed confirmation has likewise been prospectively specified but not executed. If and only if the complete Critic V4 screen passes, the full model and a matched C0-V4 baseline will be trained at each of seeds 20260908, 20260909, and 20260910 under identical budgets. Each seed must reach task-macro Spearman of at least 0.30, exceed its matched baseline by at least 0.10, satisfy the frozen MAE and task-breadth requirements, and obtain a strictly positive lower 95% bound for the task-stratified source-group paired-bootstrap Spearman difference. The three-seed median thresholds are 0.35 Spearman and 0.12 baseline margin. These are authorization rules, not observed results, and therefore add no performance claim.

The matched confirmation runtime now enforces this specification mechanically: it accepts only the full/C0 pair, records the declared seed throughout training provenance, and waits for all six terminal runs before assembling the three paired comparisons. This software readiness is not statistical readiness. No Critic V4 optimizer attempt, Validation result, confirmation result, or Development TEST access has occurred.

The subsequent V4 Development TEST decision rule is also preregistered but remains inaccessible. One atomic access may occur only after the exact three-seed gate passes. The process must evaluate the frozen three-member full-model ensemble and its three matched C0 members together, leave no reusable TEST projection or TEST bottom-encoder cache, and meet the 0.30 Spearman, 0.10 matched-baseline margin, MAE, task-breadth, and paired-bootstrap gates. This preregistration is not a TEST result; the protected read count remains zero.

The corresponding atomic runner is now implemented and locally verified. It preflights all six frozen checkpoints before consuming authorization, decodes only manifest-authorized TEST rows, keeps bottom-six TEST representations in memory, enforces exact record/target/task/source-group alignment across the three seeds, and seals the ensemble and matched-baseline comparison in one terminal decision. It has not been authorized or executed. This implementation adds no evidence about TEST performance, and all downstream refit, LOSO, guidance, and external-evaluation claims remain closed.

The post-TEST route is now prospectively specified as three fixed eight-pass all-Development refits followed by 42 paired, TEST-preserving LOSO jobs spanning seven Development studies, three seeds, and the V4 full/C0 pair. Readiness requires every seed to reach a study-macro Spearman of at least 0.25 and a C0 margin of at least 0.07, at least six positive fold margins, a positive median fold margin, a positive leave-GSE269595-out margin, and a median study-macro Spearman of at least 0.30. These are future authorization criteria only: no refit or LOSO job has been created or run, and `CRITIC_V4_READY_FOR_GUIDANCE` has not been established.

The corresponding configuration, terminal-collection, and readiness-composition software is now implemented. It derives each refit or fold update budget from the frozen sampler on that job's actual training records, requires all three refits before LOSO configuration, and converts any frozen post-TEST runtime failure into a retained NO-GO. This closes a software gap only: the training loop does not yet accept the post-TEST stages, no runtime configuration has been materialized, and the scientific claim state is unchanged.

The training loop now also implements the two post-TEST stages: refit uses all 107,873 isolated Development TRAIN/VALIDATION records without an evaluation split, whereas LOSO trains on six studies and evaluates the held-out seventh study with its unseen-study scale fixed at one. Both stages use the fixed eighth-pass checkpoint and cannot select from TEST or Validation metrics. To prevent a repeated protected-outcome read, the atomic TEST call will emit a metric-free authorization receipt; every downstream stage consumes that receipt and has no path to the sealed TEST result. This remains unexecuted software, so it does not change the scientific evidence or readiness status.

The V4 guidance authorization and state invariants are now implemented prospectively. Guidance remains blocked unless the Critic reaches `CRITIC_V4_READY_FOR_GUIDANCE` after its single atomic TEST, refit, and LOSO sequence and the generator independently reaches `XEDITSETFLOW_V4_G0_READY`. The first SetFlow confirmation seed, 20260912, is fixed as the one-shot 18-combination guidance-screen seed. The latent SetFlow mode is part of the particle state and is copied with the complete particle lineage during resampling; it cannot be selected again at each action. Rates retain the single scalar-potential-difference form, and trunk, mode, value, and each of the three critic-member forwards are charged separately against the 320-forward-equivalent ceiling. This is software and protocol evidence only: no guidance authorization, value training, SMC screen, Development TEST reread, external Evaluation read, or performance result exists.

The one-time C3-to-V4 reference producer is also implemented but has not run. It first verifies that all five frozen C3 jobs have exactly one terminal summary or technical-failure artifact, then reads the five payloads as one package and makes the output itself a rerun barrier. A valid C3 full Validation Spearman becomes the historical V4 screen reference; only a technical failure of the full arm activates the preregistered C2 full fallback. All five original artifacts are retained, and neither a successful nor failed C3 result can authorize C3 confirmation, Development TEST, refit, LOSO, or guidance. Because the producer has not executed, no C3 terminal metric has been consumed in this implementation step.

The V4 preflight and screen-launch authorization producer now connects that read-once decision to the already implemented training entry points. It requires the A100 worktree to be synchronized to the exact current HEAD only after all old jobs are terminal, requires both component-focused suites and the exact 96-test V3.3.2 cohort to pass there, and verifies the appropriate isolated cache. A Critic screen launch additionally requires the formal 165–175M parameter count, 20–35 GiB process-recorded peak allocation, and frozen physical-batch selection; a SetFlow launch additionally requires the exact full/single-mode counts and the 891-source data audit. No authorization or preflight has been produced, and no V4 parameter update has started.

The final V4 three-seed matched-generation gate is implemented as a future terminal decision. Each of seeds 20260912, 20260913, and 20260914 must satisfy the closed-neighborhood NDCG margins and positive paired intervals against both unguided SetFlow and the strongest matched baseline, reduce regret, preserve top-1 recall, meet the open-generation recovery, top-k, and diversity minima, and obtain a positive independent-evaluator interval while retaining all legality and replay guarantees. The median NDCG and evaluator margins are also gated. V4-specific checks require a trajectory-fixed mode, a single scalar potential rather than a free ratio head, and separate charging of trunk, mode, value, and each critic member. A higher critic self-score without measured and independent-evaluator improvement is explicitly classified as reward exploitation. The gate has not run and supplies no performance evidence.

The corresponding batched V4 SMC core now carries the selected latent mode inside each particle and copies that complete state during stratified resampling. It uses hard-legal SetFlow rates as proposals, applies only a single scalar-potential difference to the importance weight, and accounts separately for the shared trunk, all eight mode heads, the value model, and each terminal critic member under the frozen per-source ceiling. This closes an execution-software gap only. Guidance remains unauthorized, no value model or SMC result has been produced, protected outcomes remain unread in this stage, and no model-performance or publication claim changes.

At the latest scheduled health window (remote 20:45:58 on 2026-08-24), the C3 full arm and all four controls remained active CUDA training processes. No active performance curve, terminal metric, Development TEST outcome, or new Evaluation outcome was read, so the evidence and claim state above are unchanged.

At the subsequent scheduled health window (remote 21:46:08 on 2026-08-24), the C3 full arm and all four controls still had active processes and no terminal or failure artifact. The CUDA-memory query did not yield usable rows because its output-column order did not match the filter; no immediate supplementary observation was made and no CUDA conclusion is drawn from that missing field. No active performance curve, terminal metric, Development TEST outcome, or new Evaluation outcome was read, so the evidence and claim state remain unchanged.

The V4 value-to-go interface is now prospectively fixed as a six-block, width-384 scalar state-mode potential. The trajectory mode is an explicit state input rather than a per-action resampling decision. Each TRAIN state-mode target requires eight frozen SetFlow rollouts conditioned on that same mode and exactly the three V4 critic members; study identity and the independent evaluator are excluded. The training budget is fixed to eight passes and the final eighth-pass checkpoint. This remains unexecuted implementation and protocol evidence: no target artifact, value-model update, guidance result, protected-outcome read, or model claim has been produced.

The TRAIN-side V4 state-mode schema now derives four outcome-free states from each unique source and assigns modes by a frozen balanced ordering rule. Rollout provenance keeps that mode fixed through structural or STOP termination and requires exact alignment across all three study-neutral critic members. These schemas have not been materialized on the full data and no rollout or critic inference has run, so they add no empirical performance evidence.

The formal V4 value trainer is also implemented. It requires the exact joint readiness authorization, complete eight-mode target coverage, CUDA/BF16 execution, and the fixed eighth-pass checkpoint; it records optimizer provenance and process-measured peak memory. The runner has not been invoked, so no optimizer attempt, checkpoint, outcome access, or empirical claim exists.

The mode-fixed V4 SetFlow rollout runner is implemented separately from critic scoring. It accepts only each confirmation seed's frozen selected checkpoint, generates eight same-mode terminal trajectories per TRAIN state-mode, and repeats every batch with identical seeds to verify the complete trajectory identity before any critic inference. This runner has not been configured or executed and therefore contributes no generated artifact or empirical evidence.

The subsequent V4 rollout scorer is implemented to use exactly the three all-Development refit critic members with unknown-study scale fixed to one. It reconstructs an ephemeral bottom-six cache for generated sequence pairs, validates the complete edit bundle, and obtains one standardized prediction from each member. The trajectory mode is retained only for provenance alignment and is not a critic feature. A zero-valued dataset placeholder is explicitly marked as inference-only and is neither a measured outcome nor a model input. No scoring run has occurred, so this software adds no performance evidence.

The exact V4 value-target grid builder is now implemented prospectively. It converts replay-verified, mode-fixed TRAIN rollouts and the frozen three-member critic predictions into the six distinct uncertainty-penalty-by-temperature target packages. Guidance strength is intentionally absent from these targets: the later 18-cell screen combines the six value checkpoints with three preregistered maximum guidance strengths. The builder has not run, no target package or value checkpoint exists, and this implementation therefore adds no generation evidence or model-performance claim.

The corresponding execution-configuration producer now binds this prospective chain to the actual Critic V4 readiness and refit outputs, the frozen SetFlow confirmation runtime, and the source-level TRAIN audit. It prepares one replay-checked rollout job, one frozen-ensemble scoring job, six target packages, and six value-training jobs; the previous readiness-path mismatch was corrected before any authorization or execution. No configuration has been materialized and no job has run, so this is workflow correctness rather than empirical evidence.

The V4 scalar-potential checkpoint loader now also enforces the frozen training identity before an SMC runtime can consume a value model, including the final eighth pass, the exact uncertainty penalty and temperature, the three-member critic cohort, the eight-mode architecture, and evidence of an actual CUDA optimizer update. No formal checkpoint has been loaded and no SMC experiment has run, so this validation path does not change the claim state.

The matched-compute representation now also supports the preregistered additional 32-particle sampling rounds when a source has fewer than 32 unique candidates after its first round. Generated trajectory count is accumulated by complete 32-particle rounds, while the unique-candidate cap remains 32 and the total shared-trunk, mode-head, scalar-value, replay, root-prior, and per-member critic forwards remain bounded by 320 equivalents per source. This is an execution-semantics correction, not a relaxation of the benchmark budget or a performance result. Formal SMC has not started, protected outcomes remain unread, and the model and publication claim state is unchanged.

The formal V4 open-generation runner and its exact eighteen-cell configuration set are now implemented prospectively. All combinations share decoder seed base 20261001, select the latent mode once from the same outcome-free root prior, retain it through the complete particle lineage, repeat each sampled round for fixed-seed replay, and stop additional rounds before the 320-forward ceiling could be exceeded. Terminal critic compute is reserved separately for each ensemble member as the ceiling of 32 candidates divided by that member's frozen physical batch size; the reservation remains explicitly pending until the separate scorer actually executes it. No runtime configuration has been materialized and no generation, critic scoring, protected-outcome access, or performance result has occurred; the publication claim state is unchanged.

The corresponding terminal-candidate critic scorer is also implemented prospectively. It performs study-neutral inference separately for each source and ensemble member, reconciles the candidate-cap reservation to the actual number of physical critic batches, and combines generation and scoring wall time and peak memory under the same per-source compute record. A legal immediate-STOP identity candidate remains in the cohort and is passed to the critic's exact identity-zero path. Critic self-score is retained only as a mechanism diagnostic and cannot generate candidates or independently select a successful screen cell. No scorer configuration has been materialized and no inference or performance result exists, so the claim state is unchanged.

The V4 closed-neighborhood evaluator now marginalizes the terminal probability over the outcome-free root prior of all eight trajectory-fixed latent modes, after exactly summing every edit permutation within each mode. Legal child potentials are evaluated in fixed batches of 32 so long sequences do not create an unbounded in-memory child batch, and every such value forward is recorded. Undefined closed sources remain excluded rather than zero-filled. The companion open-support evaluator retains unknown generated outcomes as unknown and ranks by generation probability rather than diagnostic critic self-score. Neither evaluator has run or read a Validation outcome in this implementation step, so no performance or publication claim changes.

The V4 guidance screen now also has a prospectively bound independent-evaluator chain. It reuses the Development-only Siamese evaluator and strongest matched-compute generation baseline that were frozen before any V4 candidate existed, validates the evaluator's prior qualification and TRAIN-only CUDA provenance at runtime, and requires the evaluator checkpoint to differ from all three V4 guiding critic members. The evaluator runs only after generation and terminal critic diagnostics and cannot enter any model gradient. The one-shot adjudicator accepts only the complete preregistered eighteen-cell grid, reconciles every source's separately charged trunk, mode, value, and three critic-member forwards under the 320-forward ceiling, and then applies the frozen NDCG, regret, evaluator-margin, recovery, and compute ordering. This implementation has not materialized a runtime configuration or executed evaluator inference, generation metrics, or an optimizer update. It therefore supplies workflow-isolation evidence only and does not change any performance or publication claim.

The V4 matched-control core now represents unguided SetFlow, source-anchored first-order Critic guidance, and exact current-Critic simple-rate guidance with the same trajectory-fixed latent mode state as the full method. The first-order expansion is shared across modes and cached at the source, whereas the simple-rate control evaluates the frozen three-member Critic at the current and proposed states and caches each complete deterministic state reward across modes and fixed-seed replay. Critic calls are recorded as Critic calls rather than value-network calls, including when multiple sampling rounds are merged with a separate terminal-scoring reservation. This is prospective benchmark-mechanics evidence only: no matched-control GPU runner, generated cohort, metric, or performance result exists yet.

The formal V4 matched-control runner is now implemented for all three final SetFlow seeds and for unguided, first-order, simple-rate, and generate-then-rerank comparisons. It fixes the latent mode for each full particle lineage, charges primary and replay execution, and will start an additional 32-particle round only when a conservative bound that includes trajectory Critic calls and terminal scoring still fits the 320-forward ceiling. The terminal scorer preserves all trajectory Critic calls while reconciling only the separately reserved terminal batches. Generate-then-rerank is restricted to reordering the identical unguided candidate support with the frozen ensemble reward; it cannot expand or filter that support. These are prospective execution guarantees only. No matched-control configuration has been materialized, no V4 generation or Critic inference has run, protected outcome reads remain zero, and neither a model-performance nor publication claim is established.

At the scheduled remote 22:46:30 health window, the C3 full arm and all four controls still had live processes on their registered GPUs with approximately 2.12–2.19 GiB allocated per process; no exact terminal summary, failure artifact, or screen gate existed. CUDA allocation is a liveness observation, not a performance result. No active curve, log, Development TEST outcome, or new Evaluation outcome was read, so the evidence and claim state remain unchanged.

The existing V4 generation and evaluation runtimes now accept exactly the three preregistered SetFlow seeds rather than hard-coding the one-shot screen seed. This enables a future three-seed package without changing the selected guidance combination or authorizing an extra seed. Open-support reporting now distinguishes ordinary generation-score ordering from the explicit frozen-Critic ordering used by generate-then-rerank, while preserving the same candidate support and leaving unknown outcomes unknown. No final configuration has been materialized and no generation, metric, evaluator, or optimizer run has occurred; this is runtime-boundary evidence only and does not establish a model or publication claim.

The complete V4 closed-method software chain is now defined on one common measured neighborhood. Full soft-value and unguided SetFlow use exact edit-permutation terminal probabilities marginalized over the same outcome-free eight-mode root prior; the unguided path uses a strict zero potential and no value checkpoint. First-order guidance is scored by its source-anchored additive single-edit Critic potential, while simple-rate guidance and generate-then-rerank use the exact frozen ensemble terminal reward. The strongest baseline reuses its pre-V4 frozen score table. All frozen-score variants then use the same source-level NDCG, normalized-regret, and top-1 definitions, with undefined sources excluded rather than assigned zero. No closed scorer or metric has run and no measured Validation result has been consumed in this implementation step, so no performance or publication claim changes.

The prospective three-seed V4 generation graph is now fully configuration-bound. The guidance-screen seed reuses its already selected final-pass scalar-value checkpoint, whereas each of the other two preregistered SetFlow seeds must produce its own fixed-mode TRAIN rollouts, frozen three-member Critic scores, value target at the screen-selected uncertainty penalty and temperature, and fixed-pass value checkpoint. The scorer now rejects any mismatch among the configured SetFlow seed, rollout summary, individual terminal rows, and score summary. All five V4 generation methods then use the same decoder stream, candidate cap, and matched-compute ceiling; terminal Critic scoring is reconciled for every method, but only generate-then-rerank may use it to change order and it cannot change candidate support. The closed and independent-evaluator paths are also fixed to the common measured neighborhood and pre-V4 comparators. This software has not been materialized or executed: no value optimization, generation, Critic inference, Validation metric, Development TEST reopen, or new Evaluation read occurred. It therefore adds no empirical support for a model-performance or publication claim.

The final V4 evidence and adjudication path is also implemented prospectively. Five V4 methods must supply per-source compute records after terminal Critic reservations have been reconciled, with shared-trunk, latent-mode, scalar-value, and each Critic-member forward separately charged; the frozen strongest genetic baseline contributes only a non-reselected adapter and same-cohort A100 timing. The six methods must share the exact closed measured-source support, preserve undefined NDCG rather than replace it with zero, and supply source-paired bootstrap intervals for both NDCG improvements and the independent-evaluator margin. Only the exact 20260912/20260913/20260914 evidence rows at the screen-selected guidance combination can reach the terminal V4 gate. Missing evidence cannot be imputed and a failed gate remains a terminal no-go. None of these terminal artifacts has been produced and the gate has not run, so this implementation changes neither the empirical evidence nor the publication-readiness conclusion.

The post-screen full-SMC and exact closed-probability runtimes now also verify the terminal guidance-screen decision itself before execution, rather than merely checking that a supplied hyperparameter tuple belongs to the preregistered grid. The full and unguided final paths must match the uniquely selected uncertainty penalty, temperature, and maximum guidance strength; the eighteen screen jobs remain executable before that gate exists. This is a prospective runtime-freeze guarantee only and supplies no performance evidence.

At the scheduled remote 23:47:25 health window, the C3 full arm and all four controls remained alive on their registered GPUs with approximately 2.12–2.19 GiB allocated per process. None of the five exact terminal summaries, failure artifacts, or the screen gate existed. No active curve, log, Development TEST outcome, or new Evaluation outcome was read. This is liveness evidence only and leaves both the V4 empirical claim and publication-readiness state unchanged.

The prospective V4 preflight authorization now requires affirmative evidence that the A100 Critic and SetFlow focused cohorts actually passed at the exact synchronized Git HEAD, as well as a clean post-sync remote worktree and the exact 96-test V3.3.2 cohort. A zero-test audit with zero reported failures can no longer authorize parameter or memory preflight. This strengthens execution provenance only: no A100 synchronization, cache build, preflight, parameter update, Validation result, or protected-outcome read occurred, so it adds no model-performance evidence.

The V4 preflight and training runtimes now bind the actual cached tensors—not only their sidecar summaries—to the frozen mRNABERT revision and representation geometry. The Critic receipt fixes the record and sequence counts, 768-dimensional bottom-six representation, encoder-block scope, 1000/64 chunk policy, radius-32 context, and special-token offset; the SetFlow receipt fixes the 84,218-record, 19,303-source, 2,817,781-token source cache and its complete-chunk policy. These receipts are carried into terminal summaries. No cache was rebuilt or consumed on A100 and no parameter or outcome was read, so this is provenance evidence rather than performance evidence.

The Critic payload receipt specifically verifies all 43,730 unique sequence entries from the tensor payload itself, rather than relying on the sidecar count. This closes a representation-identity gap but does not constitute empirical model evidence.

Every V4 screen, confirmation, and Critic post-TEST training consumer now validates the frozen cache identity receipt produced from the actual tensor payload during formal preflight. A PASS-shaped preflight without representation identity can no longer authorize a parameter update. This change is provenance and isolation evidence only: no cache was materialized, no preflight or optimizer ran, and no Validation, Development TEST, or new Evaluation performance result was produced. It therefore does not change the model-performance or publication-readiness claim.

At the scheduled remote 00:47:08 health window on 2026-08-25, the C3 full arm and all four controls remained active CUDA processes. None of the five exact terminal summaries, failure artifacts, or the screen gate existed, so the current-HEAD read-once producer was not executed and no terminal result was opened. No active curve, Development TEST outcome, or new Evaluation outcome was read. This is liveness evidence only and leaves the V4 empirical and publication-readiness claims unchanged.

All nine prospectively named V4 behavior interfaces now have concrete implementations and real producers or consumers. The newly closed interfaces bind the Critic physical batch, endpoint-semantic expert mixture, and three-seed study-neutral prediction/reward record, as well as the SetFlow source batch, per-candidate mixture target, and terminal checkpoint decision. This is software-contract evidence only. No V4 cache, preflight, optimizer, inference, Validation metric, Development TEST result, or new Evaluation result was produced, so it does not strengthen the empirical model or publication claim.

The Critic V4 formal preflight and common training runner now enforce the preregistered physical GPU scope of devices 0–5 before CUDA initialization, together with the frozen BF16-only and no-CPU-fallback policy. This common path covers screen, confirmation, all-Development refit, and LOSO; the existing prohibition on CUDA device remapping and the A100 device check remain in force. Local focused, complete Critic V4, and exact V3.3.2 regression cohorts passed. This is execution-boundary evidence only: no GPU preflight, cache materialization, optimizer update, inference, Validation metric, Development TEST outcome, or new Evaluation outcome was produced, so the empirical performance and publication-readiness conclusions are unchanged.

The new Critic bottom-six cache builder and the read-only SetFlow V3 source-cache adoption verifier now require an explicit cache-stage authorization that can be produced only after all five C3 jobs are terminal, their summaries have been consumed by the read-once adjudicator, the old launch processes have ended, and the A100 worktree has been cleanly fast-forwarded to the exact current Git HEAD with both V4 focused cohorts and the exact 96-test V3.3.2 cohort passing. The Critic builder validates the authorization and Git identity before loading a projection and enforces physical GPUs 0–5, BF16 inference, no CPU fallback, and no CUDA device remapping. Because the 4.33-GB SetFlow V3 source-token cache is already a terminal artifact, V4 does not rebuild or overwrite it; the adoption verifier instead validates the legacy summary-to-payload binding and the actual tensor identity, then emits a separate current-HEAD receipt with zero encoder forwards and zero parameter updates. This is prospective execution-order and provenance evidence only. Neither authorization, the Critic cache, nor the SetFlow adoption receipt has been materialized, no optimizer or inference result exists, and protected outcome reads remain zero; therefore the model-performance and publication-readiness claims remain unchanged.

The formal Critic V4 preflight now includes the previously pending CUDA cache/online equivalence gate. Eight outcome-free, length-stratified unique-sequence indices spanning the shortest through longest cached sequences are reconstructed from the actual float16 chunk cache and re-encoded through the same bottom-six mRNABERT forward and attention backend. The maximum and mean absolute-difference tolerances are fixed at 0.02 and 0.005, respectively. A mismatch produces a terminal preflight pause before parameter-count or memory measurement, and screen authorization requires an explicit equivalence pass. This remains prospective software evidence: the cache and CUDA comparison have not been materialized, no Validation metric or protected outcome has been read, and no model-performance or publication claim changes.

The V4 preflight and screen authorizers now consume the cache builders' launch provenance rather than treating it as an unused sidecar annotation. Both component summaries must report the exact authorizing Git HEAD, component-specific cache-launch authorization status, a physical GPU in 0–5, a nonempty CUDA device identity, BF16 inference, and no CPU fallback. A legacy or stale-HEAD summary cannot authorize preflight even if its tensor geometry appears valid. This is prospective provenance enforcement only; no cache or parameter update has run and it adds no empirical model evidence.

The Critic V4 screen adjudicator now publishes its single terminal gate by writing a sibling partial file and atomically replacing the final path. An interrupted or pre-existing partial is rejected rather than overwritten, and an existing terminal gate remains immutable. This closes terminal-artifact integrity for both technical-failure NO-GO and metric adjudication without changing any frozen threshold, comparator, control, or selection rule. The adjudicator has not run, no metric has been read, and this software correction adds no model-performance or publication-readiness evidence.

The corresponding SetFlow V4 post-training chain now applies the same atomic terminal publication rule to every fixed checkpoint-validation summary, checkpoint-validation failure, and the single screen gate. Existing final or partial artifacts are rejected rather than overwritten, preserving the exact-one-terminal-artifact premise required by the frozen eight-checkpoint adjudication. No Validation generation or adjudication has run, and no selection rule, generation metric, or empirical claim changes.

The two V4 confirmation configuration producers now publish each exact three-seed config-plus-manifest package as one directory-level atomic operation. A complete sibling staging directory is required before the final runtime-config root appears, while existing final, staging, or run roots remain fail-closed. This preserves the preregistered Critic and SetFlow seed/model cohorts without authorizing either confirmation package. No configuration was materialized, no optimizer ran, and no empirical claim changes.

The common Critic and SetFlow V4 training entry points now atomically publish either a successful terminal summary or a technical-failure artifact and never add a failure after a success summary already exists. This exact-one-terminal rule applies to the shared screen and confirmation paths and to the Critic refit/LOSO stages. It changes no training objective or metric and supplies no empirical performance evidence because none of these V4 training runs has started.

The first executable V4 artifact stage now also publishes complete cache and preflight packages atomically. The Critic bottom-six tensor and its terminal summary appear together, the Critic preflight PASS/PAUSE is a single atomic file, and the SetFlow source-level audit and preflight appear together. This removes interrupted half-package states without changing representations, capacity measurements, or gates. None of the packages has been materialized and no performance evidence is added.

At the scheduled remote 02:48:06 health window on 2026-08-25, the C3 full arm and all four controls remained active CUDA processes on their registered GPUs. No exact terminal summary, failure artifact, C3 reference, or screen gate existed, so the read-once producer did not execute. No active performance output or protected outcome was read; this liveness observation leaves the V4 empirical and publication-readiness claims unchanged.

The unique Critic V4 frozen-TEST runner now atomically publishes its authorization-consumption marker, terminal result, post-TEST authorization receipt, and technical-failure artifact, and it cannot add a failure after the terminal result exists. Its substantive isolation remains unchanged: the full and matched C0 ensembles are evaluated in one authorized call, TEST rows and bottom-six features remain ephemeral, and no general TEST loader is retained. The runner remains unauthorized and unexecuted, so the Development TEST access count is still zero and no empirical claim is added.

The prospective all-Development refit and TEST-preserving LOSO runtimes now consume the same formal Critic V4 preflight artifact produced by the screen stage; an obsolete nonproduced path has been removed. Their exact three-refit and forty-two paired-LOSO configuration packages are also published atomically as directories. This is post-TEST execution correctness only: frozen TEST remains unauthorized, neither package exists, and no model-performance evidence changes.

Guidance readiness now requires the complete authorizing post-TEST receipt rather than only its schema. The receipt must identify the exact three confirmation seeds, a passing frozen TEST, authorization for all-Development refit, exactly one TEST access event, no persisted TEST projection or cache, no embedded TEST metrics, and no new Evaluation access. This closes a prospective authorization gap only; readiness has not been materialized and no empirical claim changes.

The joint V4 guidance authorizer now consumes the exact Critic-readiness and SetFlow-confirmation receipt schemas rather than accepting status-shaped mappings. The SetFlow receipt must contain all and only the three preregistered confirmation seeds, with every seed passed, while continuing to prohibit an additional seed, premature Development TEST access, or an already-issued component-level guidance authorization. This is prospective workflow-integrity evidence only: no joint authorization, guidance screen, model inference, Development TEST access, or new Evaluation read occurred, so it does not strengthen any model-performance or publication-readiness claim.

The prospective Critic V4 all-Development refit and TEST-preserving LOSO protocol now references the same two frozen TRAIN/VALIDATION projection files used by the screen and bottom-six cache. Two nonproduced filename variants were removed, preventing the three refit and forty-two paired LOSO jobs from failing after a legitimate frozen-TEST pass. No projection content, split assignment, target, model, or gate changed, and neither frozen TEST nor a post-TEST optimizer was run; this is execution-correctness evidence only and adds no empirical support.

All downstream V4 value-training and final-generation test fixtures now use the canonical Critic-readiness and SetFlow-confirmation receipt schemas, including all and only the three passed SetFlow confirmation seeds and the frozen no-extra-seed/no-premature-TEST boundaries. The integrated V4 software cohort passes without weakening the production authorizer. This is regression-test evidence only; it records no model training, metric, or protected-outcome access and therefore has no effect on the manuscript's empirical claims.

At the scheduled remote 03:48:31 health window on 2026-08-25, the C3 full arm and all four controls remained live CUDA processes on their registered GPUs. None of the five exact terminal summaries, failure artifacts, the C3 reference, or the screen gate existed, so the read-once producer did not execute. No active curve, log, Development TEST outcome, or new Evaluation outcome was read. This liveness-only observation leaves the V4 empirical claim and publication-readiness conclusion unchanged.

At the scheduled remote 04:50:11 health window on 2026-08-25, the C3 full arm and all four controls again remained live CUDA processes on their registered GPUs. None of the five exact terminal summaries, failure artifacts, the C3 reference, or the screen gate existed, so the read-once producer did not execute. No active curve, log, Development TEST outcome, or new Evaluation outcome was read. This liveness-only observation leaves the V4 empirical claim and publication-readiness conclusion unchanged.

At the next observed remote health point, 12:03:13 on 2026-08-25, the C3 full arm and all four controls remained live CUDA processes; no intermediate missed windows were backfilled. None of the five exact terminal summaries, failure artifacts, the C3 reference, or the screen gate existed, so the read-once producer did not execute. No active curve, log, Development TEST outcome, or new Evaluation outcome was read. This liveness-only observation leaves the V4 empirical claim and publication-readiness conclusion unchanged.

At the remote 13:03:23 health point on 2026-08-25, the C3 source-only control had naturally published its terminal summary and its registered GPU process had ended, while the full arm and the other three controls remained live CUDA processes. The source-only payload was deliberately not opened because the preregistered read-once adjudication waits for all five jobs to become terminal. No performance metric, active curve, log, Development TEST outcome, or new Evaluation outcome was read. This partial-terminal observation supplies no V4 empirical evidence and leaves publication readiness unchanged.

The C3-to-V4 reference producer now durably marks terminal consumption before opening any of the five payloads. All five terminal paths must first resolve without reading content; exclusive marker creation then precedes every JSON open, and an interrupted run with a marker but no final reference cannot automatically reread the summaries. The marker contains paths and terminal kinds but no metric or payload content. This closes execution semantics only: at implementation time one of five C3 jobs was terminal, no terminal payload or protected outcome was read, and no C3 reference or V4 performance result was created. The empirical and publication-readiness conclusions therefore remain unchanged.

The post-C3 A100 exact-current-HEAD synchronization and test executor is now versioned with the Route 2 code rather than maintained as a second project-external Python copy. Its Critic cohort explicitly includes the C3 read-once producer regression and the synchronization executor's own barrier tests, in addition to the existing Critic, SetFlow, and exact V3.3.2 cohorts. The executor still requires all five exact terminal artifacts, the completed C3 read-once reference, no surviving launch-head process, a clean remote worktree, fast-forward-only synchronization to the requested commit, and passing tests before it can emit cache-stage evidence. This executor was not run on A100 and adds no model-performance or publication evidence.

The V4 cache job runner now preserves exactly one terminal artifact when a builder has atomically published its complete summary but the process subsequently returns nonzero during output or teardown. In that case the return code remains in the runtime record, while no second failure artifact is created; only a run without a published summary can produce the technical-failure terminal. The runner is versioned in the Route 2 repository and is invoked from the synchronized A100 current HEAD. This is prospective execution-correctness evidence only: neither cache job ran, no representation or metric was produced, and protected-outcome reads remained zero.

The two-component V4 cache launcher is now also versioned in the exact A100 current HEAD. It requires the completed C3 read-once reference and matching A100 test audit, stages both component authorizations as one package, and validates the authorizer's final JSON for the exact component, status, and Git commit rather than relying on a stdout substring. The Critic path still builds the new bottom-six cache, whereas the SetFlow path only adopts the terminal V3 source-token cache read-only. This launcher was not executed, so it adds neither representation nor performance evidence and does not alter publication readiness.

The cache-to-preflight transition is now versioned under the same exact-current-HEAD rule. An atomically published Critic PASS/PAUSE or SetFlow PASS output is the unique preflight terminal even if process teardown later returns nonzero; only absence of that output can create a technical-failure terminal. The two component authorizations are validated from their final JSON for the exact component, status, and Git commit, and the A100 focused cohorts now exercise the formal cache and preflight launch chain before an authorization audit can be emitted. This is prospective execution-correctness evidence only. The preflights were not run, no capacity or memory measurement was observed, no parameter update occurred, and protected outcome reads remain zero; therefore no model-performance or publication claim changes.

The frozen V4 screen package launcher and six-GPU queue scheduler are now likewise versioned in the exact synchronized A100 HEAD. Component authorizations are accepted only after their terminal JSON records the correct component-specific status, Git commit, and complete preregistered run set. The scheduler retains the prospectively fixed queue layout and continues each later control after an earlier terminal failure, while requiring exactly one model-specific summary or failure artifact for every job. This is software execution evidence only: no screen job, optimizer update, Validation result, or protected-outcome read occurred, so it contributes no empirical support for a model-advantage or publication-readiness claim.

The post-screen coordinator is now also versioned at the frozen screen-launch HEAD. Once a Critic or SetFlow gate producer atomically publishes its PASS or NO-GO document, that gate remains the unique adjudication terminal even if the process later exits nonzero while emitting output; the return code is retained only as a diagnostic. This does not alter any metric, threshold, checkpoint-validation result, confirmation authorization, or NO-GO decision. The coordinator has not run and no gate, Validation outcome, model update, or protected outcome was produced, so this correction supplies no empirical model evidence and leaves publication readiness unchanged.

The prospective V4 confirmation training launcher is now bound to each component's terminal screen gate. A Critic PASS produces exactly the three preregistered training seeds, each with the full model and matched C0-V4 baseline; a SetFlow PASS produces exactly its three preregistered full-model seeds. A component-level NO-GO produces no configuration, authorization, or optimizer job, and neither component can authorize an additional seed or Development TEST access. The six-GPU scheduler retains exact summary-or-failure terminals and continues later queued work after a terminal failure. This is implementation evidence only: no confirmation package was materialized, no optimizer ran, and no Validation or protected outcome was read, so it does not support an empirical model claim.

At the remote 14:04:10 health point on 2026-08-25, the C3 source-only terminal summary remained unopened and the full arm plus the other three controls remained live CUDA processes. No terminal payload, active performance curve, log, Development TEST outcome, or new Evaluation outcome was read. The C3 package therefore remained one-of-five terminal, read-once adjudication did not run, and this health observation adds no empirical evidence or publication claim.

The post-training confirmation coordinator is now implemented prospectively. It accepts only an exact terminal training package at the frozen screen-launch commit. Critic confirmation is adjudicated once from all six preregistered full-versus-C0 runs. SetFlow evaluates passes 4, 6, 8, and 10 only for each successfully trained preregistered seed and then forms its terminal three-seed gate; a failed training seed is not replaced and cannot trigger fabricated checkpoint validation. Atomic validation summaries and PASS/NO-GO gates remain terminal even after a later nonzero process exit. This implementation has not run, authorizes neither Development TEST nor an additional seed, and adds no empirical support for model performance or publication readiness.

The unique Critic V4 Development TEST now also has a formal post-confirmation launch boundary. A missing Critic confirmation component, a technical adjudication failure, or a three-seed NO-GO records a no-launch decision; only the exact three-seed PASS can start the full-plus-C0 atomic evaluation. Device selection is limited to GPUs 0–5 and occurs before authorization consumption, while any failure after launch becomes a no-automatic-retry terminal. The wrapper inspects only terminal-file existence and never opens TEST metrics. This launcher has not run, the three-seed gate does not yet exist, and the protected TEST access count therefore remains zero; no empirical or publication claim changes.

The all-Development refit stage now has a separate formal launch boundary after the atomic TEST. It consumes only the metric-free post-TEST authorization receipt; a failed or nonpassing TEST creates no refit job. A passing receipt authorizes exactly the three preregistered full-model seeds for eight fixed passes with final-pass checkpointing. GPU capacity is checked before any runtime configuration or launch authorization is materialized, and the three refits are adjudicated once after exact terminals. LOSO remains closed unless all three refits complete. This code has not run and neither reopens TEST outcomes nor adds performance evidence.

The TEST-preserving LOSO and Critic-readiness chain now has a formal successor boundary after all three refits. It creates exactly 42 paired runs spanning the three fixed seeds, seven held-out studies, and full-versus-C0 models, with unknown held-out study scale fixed at one. Available GPUs 0–5 affect queue throughput only. After exact job terminals, LOSO is adjudicated once and the frozen predecessors are composed into either `CRITIC_V4_READY_FOR_GUIDANCE` or a terminal not-ready state. The chain has not executed, does not reopen TEST outcomes, and supplies no empirical model evidence.

The joint V4 guidance authorization now also has a formal exact-HEAD execution boundary. It consumes only the terminal Critic LOSO readiness and SetFlow three-seed confirmation receipts. Any scientific NO-GO, ineligible component, or technical terminal produces a no-authorization decision and launches no value model, guidance screen, SMC, or candidate generation. Only simultaneous `CRITIC_V4_READY_FOR_GUIDANCE` and `XEDITSETFLOW_V4_G0_READY` receipts can invoke the existing one-shot authorizer, which again records that Development TEST is not reopened and new Evaluation outcomes remain unread. This boundary has not executed and contributes no empirical model-performance or publication-readiness evidence.

The user selected the preregistered operational option to close the obsolete C3 package and proceed directly to V4. Before any stop signal was sent, exact process-identity resolution showed that the four previously active trainers had already exited without publishing terminal artifacts; consequently, no signal was sent and the four runs were closed as technical missing-terminal failures rather than model-performance failures. The source-only terminal summary was preserved unchanged. The single permitted read-once found no valid C3-full metric, observed source-only task-macro Spearman of -0.03240671978468869, and therefore froze the predeclared highest valid V3 full diagnostic C2 value, 0.10426561121126687, as the C3 reference for the V4 screen. C3 confirmation, Development TEST, refit, LOSO, and guidance remain unauthorized. This historical result is not evidence of a Critic advantage and does not improve publication readiness.
